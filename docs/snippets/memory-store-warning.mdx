<Warning>
  The default store is in-memory (`createTestStore`). Goals, the ledger, and the
  audit trail vanish when the process restarts. Switch to
  [`createPostgresStore`](/production/storage) before deploying.
</Warning>
